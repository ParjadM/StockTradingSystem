# Stock Trading System

Simple C++ console app built with Visual Studio for simulating stock trading. It lets you buy and sell stocks, track portfolio values, and manage basic trade records.

## Build

Open `StockTradingSystem.vcxproj` in Visual Studio and build the solution.

## Run

Run the `StockTradingSystem` project from Visual Studio after building.